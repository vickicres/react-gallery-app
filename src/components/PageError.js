import React from 'react';

//404 error page when url didn't match to an existing route
const PageError = () => (
  <div className="error">
    <h2>ERROR 404 - PAGE NOT FOUND</h2>
    <p>The page your looking for had no longer exists.</p>
  </div>
);

export default PageError;
